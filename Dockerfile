# مرحله ۱: استفاده از یک ایمیج پایه سبک (Nginx)
FROM nginx:alpine

# مرحله ۲: کپی کردن فایل‌های پروژه
# . به معنی همه محتویات دایرکتوری فعلی (PARGASSHIP) است.
# /usr/share/nginx/html مسیر پیش فرض Nginx برای فایل های استاتیک است.
COPY . /usr/share/nginx/html

# مرحله ۳ (اختیاری): اگر Nginx نیاز به تنظیم خاصی ندارد، نیازی به این بخش نیست.
# پورت ۸۰ پورت پیش فرض Nginx است و ما آن را برای مستندسازی اکسپوز می کنیم.
EXPOSE 80